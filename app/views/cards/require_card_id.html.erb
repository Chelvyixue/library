<% provide(:title, "借还书") %>
<h1>借还书</h1>

<div class="row">
  <div class="col-md-4 col-md-offset-4">
    <%= form_tag(card_id_path, method: :get) do %>
      <%= label_tag :id, "借书证号" %>
      <%= text_field_tag :id, nil, class: "form-control" %>
      <%= submit_tag "确定", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
