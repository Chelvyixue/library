<% provide(:title, "借书") %>
<h1>借书</h1>

<div class="row">
  <div class="col-md-4 col-md-offset-4">
    <%= form_tag(borrow_card_id_path, method: :post) do %>
      <%= label_tag :card_id, "借书证号" %>
      <%= text_field_tag :card_id, nil, class: "form-control" %>
      <%= submit_tag "确定", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
